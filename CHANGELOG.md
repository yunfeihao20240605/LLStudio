# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

---

## [v0.3.5] - 2026-09-01

### 修复 (Bug Fixes)

- 修复 Windows 打开视频时 `ffprobe.exe` 短暂弹出控制台窗口的问题
- 隐藏波形处理、录音降噪和片段音频提取调用的 `ffmpeg.exe` 控制台窗口
- 保留 FFmpeg 工具原有的输出捕获、错误处理和功能行为

### 发布说明 (Release Notes)

- 本版本优化 Windows 媒体工具的后台运行体验，不再显示 FFmpeg/FFprobe 控制台窗口

## [v0.3.4] - 2026-09-01

### 修复 (Bug Fixes)

- 修复 Windows 下选择视频后路径被错误转换为 `C:/C:/...`，导致媒体加载失败的问题
- 增强文件 URL 到本地路径的跨平台转换，保持 macOS 和 Linux 原有路径行为

### 发布说明 (Release Notes)

- 本版本修复 Windows 视频文件选择后的媒体加载问题

## [v0.3.3] - 2026-09-01

### 修复 (Bug Fixes)

- 修复 Windows 和 Linux 下“打开视频”不弹出文件选择窗口的问题
- 视频文件选择统一使用跨平台的 Qt `Platform.FileDialog`
- 隐藏尚未实现的“导入字幕”菜单项，避免点击后无响应

### 发布说明 (Release Notes)

- 本版本完善 Windows、macOS 和 Linux 的视频文件选择流程，并暂时隐藏未完成的字幕导入入口

## [v0.3.2] - 2026-08-31

### 优化 (Improvements)

- Windows 安装器版本由 Cargo workspace 版本自动同步，避免安装器版本号滞后
- Windows 发布构建明确使用 release 模式，并加强 Qt 平台插件和 QML 模块检查
- Windows 发布构建固定 FFmpeg/mpv 版本，并对下载资产执行 SHA-256 校验
- Windows MSVC 构建启用 mpv OpenGL 视频渲染器，同时保留 macOS/Linux 现有渲染路径

### 修复 (Bug Fixes)

- 修复 Windows 双击启动时出现控制台窗口的问题
- 修复 Windows 下视频渲染器返回空指针导致的启动和渲染风险

### 发布说明 (Release Notes)

- 本版本重点修复 Windows 启动与安装包运行时依赖问题，并统一安装器版本同步规则

---

## [v0.3.0] - 2026-08-30

### 新增 (Features)

- 增加录音导出功能，可将当前录音保存为 WAV 音频文件
- 在录音相关菜单和操作区域中提供录音导出入口
- 导出时默认生成包含目标时间范围的文件名，便于区分不同录音片段
- 关于 LLStudio 面板增加主题化的自定义标题栏
- 关于面板顶部显示产品名称 `LLStudio`
- 关于面板在品牌图片下方显示当前版本号，例如 `Version 0.3.0`
- 关于面板增加产品标语：以兴趣为引，以 AI 为伴，随时随地享受个性化学习

### 优化 (Improvements)

- 优化录音导出文件选择流程，支持保存位置选择和 WAV 文件类型提示
- 优化导出文件名，自动使用录音目标区间的起止时间生成默认名称
- 关于面板标题栏背景、文字和边框接入应用主题系统
- 关于面板内容区和底部按钮区使用统一的主题颜色
- 调整关于面板高度和品牌图片区域高度，为版本号和产品标语预留稳定空间
- 产品标语使用单行显示，避免在固定宽度窗口中出现不必要的换行
- 将媒体加载接口从视频语义扩展为统一媒体语义，便于视频和音频共用播放流程
- 音频文件播放时保持媒体播放器句柄有效，改善音频-only 媒体的播放状态管理

### 修复 (Bug Fixes)

- 修复默认 `Dialog.title` 使用 Qt 平台样式，导致关于面板顶部标题栏不跟随主题的问题
- 修复关于面板产品名称附带版本号后可能显示不完整的问题
- 修复音频文件没有视频画面时，播放状态和波形定位可能不同步的问题
- 修复媒体加载和波形定位请求在播放器异步准备完成前到达时的处理问题

### 发布说明 (Release Notes)

- 本版本在 `v0.2.0` 的音频播放、波形、字幕、笔记和训练能力基础上，增加录音导出和更完整的关于面板信息
- 应用版本号统一由 Cargo workspace 版本配置驱动，界面无需单独维护版本文本
- 发布构建由 GitHub Actions 根据 `v0.3.0` 标签自动生成多平台安装包

### 已知限制 (Known Limitations)

- 录音导出依赖当前录音已经生成并加载完成
- 导出格式当前为 WAV
- 音频播放和波形分析依赖打包版本中可用的 FFmpeg、ffprobe 和 libmpv 运行环境
- 某些音频格式是否能够播放，取决于对应平台提供的媒体解码组件

---

## [v0.2.0] - 2026-08-30

### 新增 (Features)

- 在“文件”菜单中新增“打开音频”功能
- 支持选择并播放 MP3、WAV、M4A、AAC、FLAC、OGG 和 OPUS 等常见音频格式
- 音频文件复用现有媒体播放后端、播放控制和波形数据处理流程
- 音频播放模式下显示音频信息卡片，并隐藏不适用的视频画面
- 音频文件可以使用播放进度、波形定位、选区和播放速度控制
- 增加可编辑的笔记工作流，支持创建、编辑、删除和按时间范围关联笔记
- 增加笔记导航，可从笔记跳转到对应的音频或视频时间位置
- 增加片段列表管理，支持创建、编辑、删除和激活学习片段
- 支持将当前选区同步为学习片段，并保留片段的循环次数和间隔设置
- 增加语音模型选择设置，支持在可用的语音识别模型之间切换
- 关于 LLStudio 面板显示当前应用版本号
- 关于面板中的版本号自动读取应用版本配置，版本更新后会自动同步

### 优化 (Improvements)

- 优化高倍率波形视图的 bin 绘制方式
- 移除连接相邻 bin 的多边形绘制逻辑，避免根据相邻数据人为插入波形走势
- 改为根据每个 bin 的 `min/max` 幅度独立绘制矩形
- 在相邻 bin 之间增加细小间隙，降低多个 bin 视觉上连成一片的问题
- 当多个 bin 被压缩到同一屏幕像素时，使用聚合波形进行显示
- 优化 preview bins 与 detail bins 的切换和细节波形刷新
- 优化波形倍率滑块、倍率步进按钮和倍率输入框
- 支持更细的倍率输入，并在倍率变化后保持当前时间位置附近的视口
- 优化字幕、片段和录音面板之间的时间范围同步
- 优化笔记编辑器、笔记列表和片段列表的主题样式
- 完善浅色和深色主题下的控件、弹窗、按钮、列表和输入框表现
- 关于面板上方内容区域跟随主面板背景颜色变化
- 关于面板下方按钮区域跟随次级背景和边框颜色变化
- 改善应用启动尺寸和启动时的窗口展示状态
- 增加稳定的 macOS 本地开发启动脚本，自动准备 Qt、FFmpeg 和 mpv 运行环境

### 修复 (Bug Fixes)

- 修复高倍率波形下连接多边形造成波形形状失真的问题
- 修复部分高倍率场景下 bins 显示不清晰的问题
- 修复细节波形数据更新后视图未及时刷新的问题
- 修复选区、字幕和学习片段时间范围不同步的问题
- 修复笔记与当前媒体时间位置关联不稳定的问题
- 修复关于面板部分背景区域不跟随主题变化的问题
- 修复关于面板版本信息需要手动维护的问题
- 修复音频文件没有视频帧时播放器生命周期和播放状态不稳定的问题
- 修复音频播放过程中波形定位请求早于媒体状态准备完成时的定位问题

### 播放与分析 (Playback and Analysis)

- 音频文件和视频文件使用统一的媒体加载接口
- 音频文件加载后继续生成对应的预览波形和细节波形数据
- 支持在音频波形上进行定位、选区和片段创建
- 音频文件继续复用现有播放速度、训练、录音对比和语音识别接口
- 高倍率波形使用细节分析数据，并避免通过插值制造不存在的波形走势
- 细节波形分析使用外部 FFmpeg 解码音频采样数据
- 播放器在音频文件没有视频画面时保持媒体句柄有效

### 发布包 (Packages)

本版本计划构建以下平台安装包：

- Windows 安装程序：`LLStudio-Setup.exe`
- Linux 安装包：`LLStudio_amd64.deb`
- macOS Apple Silicon：`LLStudio-macOS-arm64.dmg`
- macOS Intel：`LLStudio-macOS-x86_64.dmg`

### 已知限制 (Known Limitations)

- 音频播放和波形分析依赖当前打包版本中可用的 FFmpeg、ffprobe 和 libmpv 运行环境
- 某些音频格式是否能够播放，取决于对应平台打包的媒体解码组件
- 音频文件没有视频画面，因此不会显示视频内容和视频首帧
- 字幕、片段、录音和语音识别功能仍然依赖对应的字幕文件、录音数据或外部语音服务配置
- 在部分非 macOS 开发环境中，系统文件选择器和媒体运行时需要额外配置

---

## [v0.1.3] - 2026-08-27

### 新增 (Features)
- 使用统一应用图标，支持 Qt 窗口、Windows、macOS 和 Debian/Linux 桌面环境
- 增加 Windows `.ico`、macOS `.icns` 和 Linux hicolor 多尺寸图标资源

### 修复 (Bug Fixes)
- 修复 macOS 从 DMG 启动时录音目录落到只读当前工作目录导致无法创建录音的问题
- 修复录音文件删除失败时数据库记录先被删除的问题
- 移除 QML 启动阶段对应用图标资源的依赖，避免 macOS DMG 启动后窗口未显示

---

## [v0.1.2] - 2026-08-25

### 修复 (Bug Fixes)
- 修复 macOS / Windows 下 `mpv_opengl_init_params` 结构体初始化方式不兼容旧版 mpv 头文件导致的编译错误
- 修复 `media_bridge.rs` 中 `use std::process::Command` 缺失导致的 `E0433` 编译错误
- 修复 Windows 链接器 `LNK1181: cannot open input file 'mpv.lib'` 错误——`build.rs` 新增对 `MPV_PREFIX` 根目录的 `rustc-link-search`，兼容 shinchiro dev 包的目录结构

### 优化 (Improvements)
- 重命名所有发布安装包为 `LLStudio` 前缀，文件名更简洁统一
  - `LLStudio-Setup.exe`（Windows）
  - `LLStudio_amd64.deb`（Linux）
  - `LLStudio-macOS-arm64.dmg`（macOS Apple Silicon）
  - `LLStudio-macOS-x86_64.dmg`（macOS Intel）
- CI 矩阵新增 `macos-15` 与 `macos-15-intel` runner，覆盖 Apple Silicon 与 Intel 双架构

---

## [v0.1.1] - 2026-08-24

### 新增 (Features)
- 初始多平台 CI/CD 发布流程（Linux `.deb`、macOS `.dmg`、Windows `.exe`）
- 基于 mpv 的视频播放后端（`mpv_video_item.cpp`）
- QML 与 Rust 混合架构（cxx-qt 桥接层）

---

## [v0.1.0] - 2026-08-01

### 新增 (Features)
- 项目初始化
- 英语听力学习核心功能框架
- 字幕、波形、录音、笔记、AI 辅导等模块骨架
